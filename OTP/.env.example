# Server configuration
# Leave NODE_ENV unset in production—platforms like Vercel already default to
# "production" during builds and in serverless functions. Uncomment the next
# line when you explicitly need to force development behaviour locally.
# NODE_ENV=development
# PORT is ignored on Vercel because the Express app is executed inside a
# serverless function, but other hosts (Render, Railway, Fly.io, etc.) will use
# it when binding the HTTP listener.
PORT=5000

# Database & session
# `DATABASE_URL` is preferred, but the Supabase dashboard sometimes exposes the
# same string under the variable name `postgresql`. Either key will be detected
# automatically by the server.
DATABASE_URL=
postgresql=
SESSION_SECRET=

# JWT authentication
JWT_SECRET=
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=
JWT_REFRESH_EXPIRES_IN=30d
BCRYPT_ROUNDS=12

# Twilio voice calling
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_API_KEY=
TWILIO_API_SECRET=
TWILIO_APP_SID=
TWILIO_PHONE_NUMBER=
